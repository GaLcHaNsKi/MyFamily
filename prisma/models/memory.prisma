model Memory {
    id           String       @id @default(uuid())
    
    authorId     String
    author       Member         @relation("Memories", fields: [authorId], references: [id])

    participants Member[]
    addedAt      DateTime     @db.Date
    
    title        String
    description  String?      @db.Text
    
    attachments  Attachment[]
}
